<UserControl x:Class="TestTask1.Views.EditOther"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TestTask1.Views"
             xmlns:VM="clr-namespace:TestTask1.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0">Название банка</Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding EditingOther.BankName}" />
        <Label Grid.Row="1" Grid.Column="0">Имя</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EditingOther.Name}" />
        <Label Grid.Row="2" Grid.Column="0">Информация об активе</Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EditingOther.Value}" />
    </Grid>
</UserControl>